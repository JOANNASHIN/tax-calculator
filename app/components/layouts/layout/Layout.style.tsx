import { css } from '@emotion/react';
import { color } from '@/styles/variants';

const style = css`
  position: relative;
  width: 100%;
  height: 100%;
  background: ${color.bg};

  main {
    position: relative;
    width: 100%;
    height: 100%;
  }
`;

export default style;
